export interface CurrencyOption {
  name: string
  code: string
}

export interface CountryOption {
  name: string
  code: string
  flag?: string
}

export type BuildingType = {
  label: string
  value: string
}
